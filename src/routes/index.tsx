import React from "react";
import { BrowserRouter } from "react-router-dom";
import RoutesLayout from './app.layout.routes'

const Routes: React.FC = () => (
    <BrowserRouter>
        <RoutesLayout/>
    </BrowserRouter>

)
export default Routes;
  